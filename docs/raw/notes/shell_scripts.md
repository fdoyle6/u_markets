# Shell Scripting Notes

## Overview

This file outlines the general notes that I have found about shell scripting.

## Notes

- When defining variables, there is no space between the variable name, equals sign, and assignment. `varname=expression` is the only valid way to do this, `varname = expression` does not work.
